# Changelog

## 1.0.0

- Add the opt-in Constitution template and command propagation wrapper.
- Resolve installed command artifacts through the active integration manifest
  without hard-coding agent output layouts.
