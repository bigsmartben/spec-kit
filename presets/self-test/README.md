# Self-Test Preset

This repository-only preset exercises preset replacement and wrapping behavior.
It supplies deterministic test templates for the core document set, replaces
the Specify command, and adds a wrap-strategy command used by integration
tests.

The preset is not part of the default initialization stack.
